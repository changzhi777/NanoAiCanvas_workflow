# UI/UX 优化执行计划 - 方案1：渐进式优化

> 项目：NanoAiCanvas - 无限画布应用
> 优化方案：方案1 - 渐进式优化（3个阶段，6-8周）
> 创建时间：2026-04-20
> 性能目标：首屏 < 2s，交互响应 < 100ms

---

## 📋 总览

### 阶段划分

- **Phase 1**: 性能与可访问性优化（1-2周）
- **Phase 2**: 视觉与交互优化（2-3周）
- **Phase 3**: 高级特性实现（2-3周）

### 设计原则

- 保持 Base Nova 暗色主题一致性
- 遵循 WCAG 2.1 AA 可访问性标准
- 优先性能优化，再优化视觉效果
- 每个阶段独立可测试和验证

---

## Phase 1: 性能与可访问性优化（1-2周）

### 目标

- ✅ 首屏加载时间 < 2s
- ✅ 交互响应时间 < 100ms
- ✅ 完整键盘导航支持
- ✅ prefers-reduced-motion 支持
- ✅ Skeleton 加载状态

### 1.1 性能优化 - 首屏加载 < 2s

#### 步骤 1.1.1: 代码分割优化

**文件**: `vite.config.ts`

**操作**:
- 添加 `@radix-ui` 组件懒加载
- 优化现有 manualChunks 配置
- 添加 reactflow 按需加载

**预期结果**:
- 初始 bundle 减少 30-40%
- 首屏加载时间 < 1.5s

**代码示例**:
```typescript
// vite.config.ts
manualChunks: {
  'react-vendor': ['react', 'react-dom', 'react-redux'],
  'reactflow-vendor': ['reactflow'],
  'ui-vendor': [
    '@radix-ui/react-dialog',
    '@radix-ui/react-dropdown-menu',
  ],
  'icons': ['lucide-react'], // 新增：图标分离
}
```

---

#### 步骤 1.1.2: React 组件懒加载

**文件**: `src/components/panels/PropertiesPanel.tsx`
**文件**: `src/components/panels/NodeTemplatesPanel.tsx`

**操作**:
- 使用 `React.lazy()` 懒加载面板组件
- 添加 `<Suspense>` + `<Skeleton>` 包装

**预期结果**:
- 初始渲染减少 20-30%
- 面板打开时再加载

**代码示例**:
```typescript
// src/components/panels/PropertiesPanel.tsx
import { lazy, Suspense } from 'react'
import { Skeleton } from '@/components/ui/skeleton'

const PropertiesPanelContent = lazy(() => import('./PropertiesPanelContent'))

export default function PropertiesPanel() {
  return (
    <Suspense fallback={<Skeleton className="w-full h-full" />}>
      <PropertiesPanelContent />
    </Suspense>
  )
}
```

---

#### 步骤 1.1.3: 图片和资源优化

**文件**: `src/assets/` (如果有图片)

**操作**:
- 转换图片为 WebP 格式
- 添加 `srcset` 响应式图片
- 实现图片懒加载

**预期结果**:
- 图片体积减少 50-70%
- 图片加载不影响首屏

---

#### 步骤 1.1.4: 预加载关键资源

**文件**: `index.html`

**操作**:
- 添加关键 CSS 预加载
- 预加载字体文件
- 添加 DNS prefetch

**代码示例**:
```html
<!-- index.html -->
<link rel="preload" href="/src/styles/globals.css" as="style">
<link rel="preconnect" href="https://fonts.googleapis.com">
```

---

### 1.2 可访问性 - 键盘导航

#### 步骤 1.2.1: 添加 focus 状态样式

**文件**: `src/styles/globals.css`

**操作**:
- 为所有交互元素添加 focus ring
- 确保对比度 >= 3:1
- 移除默认 outline 的同时添加替代方案

**预期结果**:
- 所有按钮、链接、输入框有可见 focus 状态
- Tab 导航流畅

**代码示例**:
```css
/* src/styles/globals.css */
@layer components {
  .focus-ring {
    @apply focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 focus:ring-offset-background;
  }

  /* 为所有可交互元素添加 */
  button:focus-visible,
  a:focus-visible,
  [role="button"]:focus-visible {
    @apply focus-ring;
  }
}
```

---

#### 步骤 1.2.2: 实现 Skip Links

**文件**: `src/components/accessibility/SkipLink.tsx` (新建)

**操作**:
- 创建 SkipLink 组件
- 添加到主应用入口
- 实现跳转到主内容

**预期结果**:
- 键盘用户可跳过导航
- Tab 键快速到达主内容

**代码示例**:
```typescript
// src/components/accessibility/SkipLink.tsx
export function SkipLink() {
  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-primary focus:text-primary-foreground focus:rounded-md"
    >
      跳转到主内容
    </a>
  )
}
```

---

#### 步骤 1.2.3: 添加 ARIA 标签

**文件**: `src/components/canvas/Canvas.tsx`
**文件**: `src/components/toolbar/Toolbar.tsx`
**文件**: `src/components/panels/PropertiesPanel.tsx`

**操作**:
- 为所有图标按钮添加 `aria-label`
- 为面板添加 `role="region"` 和 `aria-label`
- 为表单元素添加 `aria-describedby`

**预期结果**:
- 屏幕阅读器可正确识别所有元素
- 符合 WCAG 2.1 AA 标准

**代码示例**:
```typescript
// src/components/toolbar/Toolbar.tsx
<Button
  variant="ghost"
  size="icon"
  aria-label="添加节点"
  onClick={handleAddNode}
>
  <Plus className="h-5 w-5" />
</Button>
```

---

### 1.3 动画优化 - prefers-reduced-motion

#### 步骤 1.3.1: 添加 reduced-motion 支持

**文件**: `tailwind.config.ts`

**操作**:
- 添加自定义媒体查询
- 创建动画工具类

**预期结果**:
- 尊重用户的动画偏好设置
- 减少动画引起的眩晕

**代码示例**:
```typescript
// tailwind.config.ts
export default {
  theme: {
    extend: {
      screens: {
        'motion-safe': '(prefers-reduced-motion: no-preference)',
        'motion-reduce': '(prefers-reduced-motion: reduce)',
      },
    },
  },
}
```

---

#### 步骤 1.3.2: 更新动画类

**文件**: 所有使用动画的组件

**操作**:
- 将 `animate-*` 类改为 `motion-safe:animate-*`
- 添加 `motion-reduce:transition-none`

**预期结果**:
- 动画在 reduced-motion 模式下禁用
- 保持功能完整性

---

### 1.4 加载状态 - Skeleton

#### 步骤 1.4.1: 创建 Skeleton 组件

**文件**: `src/components/ui/skeleton.tsx` (如果不存在)

**操作**:
- 添加 shadcn/ui Skeleton 组件
- 创建可复用的加载占位符

**预期结果**:
- 所有异步加载有视觉反馈
- 符合 shadcn/ui 规范

---

#### 步骤 1.4.2: 实现面板 Skeleton

**文件**: `src/components/panels/PropertiesPanel.tsx`
**文件**: `src/components/panels/NodeTemplatesPanel.tsx`

**操作**:
- 添加加载状态检测
- 显示 Skeleton 占位符
- 模拟实际内容布局

**预期结果**:
- 面板加载时有骨架屏
- 减少感知加载时间

**代码示例**:
```typescript
// src/components/panels/PropertiesPanel.tsx
import { Skeleton } from '@/components/ui/skeleton'

export function PropertiesPanelSkeleton() {
  return (
    <div className="p-4 space-y-4">
      <Skeleton className="h-8 w-3/4" />
      <Skeleton className="h-4 w-full" />
      <Skeleton className="h-4 w-2/3" />
      <Skeleton className="h-32 w-full" />
    </div>
  )
}
```

---

### Phase 1 验收标准

- [ ] Lighthouse 性能分数 >= 90
- [ ] 首屏加载 < 2s (Fast 3G)
- [ ] 交互响应 < 100ms
- [ ] 完整键盘导航（Tab、Enter、Esc）
- [ ] WAVE 工具无可访问性错误
- [ ] prefers-reduced-motion 模式测试通过
- [ ] 所有异步操作有加载状态

---

## Phase 2: 视觉与交互优化（2-3周）

### 目标

- ✅ 暗色主题对比度 >= 7:1 (WCAG AAA)
- ✅ 统一微交互（150-300ms）
- ✅ 统一 hover/focus 状态
- ✅ 改进 glassmorphism 效果

### 2.1 对比度优化

#### 步骤 2.1.1: 审查当前颜色对比度

**工具**: Chrome DevTools Lighthouse + Contrast Checker

**操作**:
- 检查所有文本对比度
- 记录不合规的颜色组合
- 生成对比度报告

**预期结果**:
- 完整的对比度审计报告
- 识别需要修改的颜色

---

#### 步骤 2.1.2: 更新 CSS 变量

**文件**: `src/styles/globals.css`

**操作**:
- 调整 `--foreground` 亮度
- 确保 `--muted-foreground` 对比度 >= 7:1
- 更新 `--border` 可见性

**预期结果**:
- 所有文本对比度 >= 7:1
- 边框在暗色模式下可见

**代码示例**:
```css
/* src/styles/globals.css */
:root {
  /* 调整前 */
  --foreground: 0 0% 98%; /* 对比度可能不足 */

  /* 调整后 - 确保 7:1 对比度 */
  --foreground: 0 0% 100%; /* 纯白 */
  --muted-foreground: 0 0% 70%; /* 从 65% 提升到 70% */
  --border: 168 15% 25%; /* 从 20% 提升到 25% */
}
```

---

#### 步骤 2.1.3: 更新组件颜色

**文件**: 所有组件文件

**操作**:
- 替换不合规的颜色
- 使用语义化颜色变量
- 测试实际对比度

**预期结果**:
- 所有组件符合 WCAG AAA
- 颜色使用一致

---

### 2.2 微交互统一

#### 步骤 2.2.1: 创建过渡工具类

**文件**: `src/styles/globals.css`

**操作**:
- 定义标准过渡时长
- 创建缓动函数变量
- 添加常用过渡类

**预期结果**:
- 统一的动画时长（150-300ms）
- 一致的缓动函数

**代码示例**:
```css
/* src/styles/globals.css */
@layer utilities {
  .transition-base {
    transition-duration: 200ms;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  .transition-fast {
    transition-duration: 150ms;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  .transition-slow {
    transition-duration: 300ms;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }
}
```

---

#### 步骤 2.2.2: 更新按钮交互

**文件**: `src/components/ui/button.tsx`

**操作**:
- 添加 hover 背景色过渡
- 添加 active 状态
- 添加 disabled 状态样式

**预期结果**:
- 按钮交互流畅
- 状态反馈清晰

---

#### 步骤 2.2.3: 更新卡片交互

**文件**: `src/components/ui/card.tsx`

**操作**:
- 添加 hover 阴影效果
- 添加 subtle 边框变化
- 避免 scale 变换（防止布局偏移）

**预期结果**:
- 卡片 hover 有明显反馈
- 无布局偏移

**代码示例**:
```css
/* 正确的卡片 hover */
.card-hover {
  @apply transition-base hover:shadow-lg hover:border-primary/50;
}

/* 错误示例 - 避免 */
.card-hover-bad {
  @apply hover:scale-105; /* 会导致布局偏移 */
}
```

---

### 2.3 Focus 状态统一

#### 步骤 2.3.1: 创建 focus 工具类

**文件**: `src/styles/globals.css`

**操作**:
- 定义 focus ring 样式
- 创建 focus-visible 样式
- 添加 focus offset

**预期结果**:
- 所有交互元素 focus 样式一致
- 键盘导航体验流畅

**代码示例**:
```css
/* src/styles/globals.css */
@layer utilities {
  .focus-ring-base {
    @apply focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 focus:ring-offset-background;
  }

  .focus-ring-inset {
    @apply focus:outline-none focus:ring-2 focus:ring-primary focus:ring-inset;
  }
}
```

---

#### 步骤 2.3.2: 更新所有交互组件

**文件**: `src/components/ui/*.tsx`

**操作**:
- 添加 `focus-ring-base` 类
- 测试键盘导航
- 确保 focus 顺序正确

**预期结果**:
- 所有组件有统一的 focus 状态
- Tab 顺序符合视觉顺序

---

### 2.4 Glassmorphism 改进

#### 步骤 2.4.1: 优化 glass 效果

**文件**: `src/styles/globals.css`

**操作**:
- 调整 backdrop-filter blur 值
- 优化半透明背景
- 添加细微边框

**预期结果**:
- Glass 效果在暗色模式下清晰
- 文本对比度 >= 7:1

**代码示例**:
```css
/* src/styles/globals.css */
@layer components {
  .glass {
    @apply bg-card/80 backdrop-blur-md border border-border/50;
  }

  .glass-strong {
    @apply bg-card/90 backdrop-blur-lg border border-border/70;
  }
}
```

---

#### 步骤 2.4.2: 应用到面板组件

**文件**: `src/components/panels/PropertiesPanel.tsx`
**文件**: `src/components/panels/NodeTemplatesPanel.tsx`

**操作**:
- 使用 glass 效果
- 测试可读性
- 调整透明度

**预期结果**:
- 面板有现代感
- 内容清晰可读

---

### Phase 2 验收标准

- [ ] 所有文本对比度 >= 7:1 (WCAG AAA)
- [ ] 所有交互元素有 hover 状态
- [ ] 动画时长在 150-300ms 范围
- [ ] Focus 状态清晰可见
- [ ] Glass 效果不干扰可读性
- [ ] 无布局偏移（CLS < 0.1）
- [ ] 用户测试反馈正面

---

## Phase 3: 高级特性实现（2-3周）

### 目标

- ✅ 响应式布局优化（375px - 1440px+）
- ✅ 高级动画效果
- ✅ 自定义 cursor（可选）

### 3.1 响应式布局

#### 步骤 3.1.1: 审查当前响应式

**工具**: Chrome DevTools 设备模拟器

**操作**:
- 测试所有断点（375px, 768px, 1024px, 1440px）
- 记录布局问题
- 生成响应式问题清单

**预期结果**:
- 完整的响应式问题报告
- 优先级排序

---

#### 步骤 3.1.2: 优化移动端布局

**文件**: `src/components/canvas/Canvas.tsx`
**文件**: `src/components/panels/*.tsx`

**操作**:
- 实现移动端面板折叠
- 优化触摸目标（>= 44x44px）
- 调整字体大小（>= 16px）

**预期结果**:
- 移动端可用性提升
- 触摸操作友好

**代码示例**:
```typescript
// src/components/panels/PropertiesPanel.tsx
export default function PropertiesPanel() {
  const [isMobile, setIsMobile] = useState(false)

  useEffect(() => {
    const checkMobile = () => setIsMobile(window.innerWidth < 768)
    checkMobile()
    window.addEventListener('resize', checkMobile)
    return () => window.removeEventListener('resize', checkMobile)
  }, [])

  if (isMobile) {
    return <PropertiesPanelMobile />
  }

  return <PropertiesPanelDesktop />
}
```

---

#### 步骤 3.1.3: 添加响应式工具类

**文件**: `tailwind.config.ts`

**操作**:
- 自定义断点
- 添加响应式工具类
- 优化容器宽度

**预期结果**:
- 响应式开发更高效
- 布局一致性提升

---

### 3.2 高级动画

#### 步骤 3.2.1: 安装动画库

**操作**:
```bash
pnpm add framer-motion
```

**预期结果**:
- 动画库可用
- TypeScript 类型支持

---

#### 步骤 3.2.2: 实现页面过渡

**文件**: `src/components/animations/PageTransition.tsx` (新建)

**操作**:
- 创建页面切换动画
- 使用 Framer Motion
- 添加淡入淡出效果

**预期结果**:
- 页面切换流畅
- 性能开销小

**代码示例**:
```typescript
// src/components/animations/PageTransition.tsx
import { motion } from 'framer-motion'

export function PageTransition({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      transition={{ duration: 0.2 }}
    >
      {children}
    </motion.div>
  )
}
```

---

#### 步骤 3.2.3: 实现列表动画

**文件**: `src/components/panels/NodeTemplatesPanel.tsx`

**操作**:
- 添加列表项进入动画
- 使用 stagger 效果
- 优化性能

**预期结果**:
- 列表加载有动画
- 视觉流畅

---

#### 步骤 3.2.4: 实现微交互

**文件**: 各组件文件

**操作**:
- 添加按钮点击反馈
- 添加卡片 hover 效果
- 添加通知动画

**预期结果**:
- 交互反馈清晰
- 提升用户体验

---

### 3.3 自定义 Cursor（可选）

#### 步骤 3.3.1: 创建自定义 Cursor

**文件**: `src/components/ui/CustomCursor.tsx` (新建)

**操作**:
- 创建 cursor 组件
- 实现 magnetic 效果
- 添加 hover 变形

**预期结果**:
- 自定义 cursor 可用
- 性能良好

**代码示例**:
```typescript
// src/components/ui/CustomCursor.tsx
import { useEffect, useState } from 'react'
import { motion } from 'framer-motion'

export function CustomCursor() {
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 })

  useEffect(() => {
    const updateMousePosition = (e: MouseEvent) => {
      setMousePosition({ x: e.clientX, y: e.clientY })
    }

    window.addEventListener('mousemove', updateMousePosition)
    return () => window.removeEventListener('mousemove', updateMousePosition)
  }, [])

  return (
    <motion.div
      className="fixed top-0 left-0 w-4 h-4 border-2 border-primary rounded-full pointer-events-none z-50 hidden md:block"
      animate={{ x: mousePosition.x - 8, y: mousePosition.y - 8 }}
      transition={{ type: 'spring', stiffness: 500, damping: 28 }}
    />
  )
}
```

---

#### 步骤 3.3.2: 添加 cursor 状态管理

**文件**: `src/contexts/CursorContext.tsx` (新建)

**操作**:
- 创建 cursor 状态 context
- 添加 hover 状态
- 实现 cursor 变形

**预期结果**:
- Cursor 状态全局管理
- 交互反馈清晰

---

### Phase 3 验收标准

- [ ] 所有断点布局正常（375px - 1440px+）
- [ ] 触摸目标 >= 44x44px
- [ ] 动画流畅（60fps）
- [ ] 自定义 cursor 可选开关
- [ ] 移动端体验良好
- [ ] 性能无回退

---

## 📊 总体验收标准

### 性能指标

- [ ] Lighthouse 性能分数 >= 90
- [ ] 首屏加载 < 2s (Fast 3G)
- [ ] 交互响应 < 100ms
- [ ] CLS < 0.1
- [ ] FID < 100ms
- [ ] LCP < 2.5s

### 可访问性指标

- [ ] Lighthouse 可访问性分数 = 100
- [ ] WCAG 2.1 AA 合规
- [ ] 完整键盘导航
- [ ] 所有文本对比度 >= 7:1
- [ ] WAVE 工具无错误

### 视觉和交互

- [ ] 设计系统文档完成
- [ ] 组件库统一
- [ ] 动画流畅（150-300ms）
- [ ] 响应式布局完善
- [ ] 用户测试反馈正面

---

## 🚀 执行注意事项

### 优先级原则

1. **性能优先**: Phase 1 必须完成才能进入 Phase 2
2. **可访问性不妥协**: 对比度、键盘导航是硬性要求
3. **渐进增强**: Phase 3 是锦上添花，可适当调整

### 测试要求

- 每个步骤完成后进行本地测试
- 使用 Lighthouse、WAVE、axe DevTools 验证
- 在不同浏览器测试（Chrome、Firefox、Safari）
- 在不同设备测试（桌面、平板、手机）

### 回归测试

- 每个阶段完成后执行完整回归测试
- 确保新功能不破坏现有功能
- 性能指标不能回退

### 文档更新

- 更新 CLAUDE.md 文档
- 记录设计决策
- 更新组件使用说明

---

## 📝 变更记录

### 2026-04-20
- 创建执行计划
- 定义 3 个阶段的详细步骤
- 设置验收标准

---

**计划制定完成，等待用户批准后开始执行** ✅

# NanoBanana2 (nani2d) 502 错误修复

## 问题
- P1: V2 Image API Provider 异常静默吞掉，创建幽灵任务
- P2: Nginx `location = /nano2` 精确匹配不兼容 `/nano2/`
- P3: WuyinkejiProvider 认证方式不一致

## 计划
1. 修复 `backend/app/api/v2/image.py` — Provider 异常返回 502
2. 修复 `frontend.Dockerfile` — `/nano2` 前缀匹配
3. 修复 `backend/app/providers/wuyinkeji.py` — 统一认证方式

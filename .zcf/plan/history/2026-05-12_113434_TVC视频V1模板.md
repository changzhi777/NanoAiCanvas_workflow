# TVC视频V1版模板

**创建时间**: 2026-05-12 07:11:54
**状态**: 执行中

## 工作流设计

```
文案/剧本生成 → 分镜头故事板 → 视频生成(并行) → 音频合成(BGM+配音) → 输出
```

5个节点，TVC广告视频生成专业流程。

## 执行步骤

1. 创建 `StoryboardVideoNode` 组件（card-node样式 + API分发）
2. 注册节点类型 `nodes/index.ts`
3. 添加枚举 `STORYBOARD_VIDEO` + `executeNode` case
4. 创建 TVC 视频 V1 模板文件 `templates/tvcVideo01.ts`
5. 注册模板到 `BUILT_IN_TEMPLATES`
6. 属性面板支持

## StoryboardVideoNode 设计

- 参照 ImagePreviewNode 的 `card-node node-appear node-end` 样式
- 顶部 `node-type-strip` 彩色条
- 头部：Video 图标 + 标题 + 状态 Badge
- 可折叠设置：API提供商(MiniMax/GLM/即梦) + 模型 + 时长
- 视频缩略图网格 + P编号
- 并行批量视频生成
- 加载态脉冲动画
- 底部操作栏 + 下载全部

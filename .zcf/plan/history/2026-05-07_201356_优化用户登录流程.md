# 优化用户登录流程

## 任务描述
优化用户注册→审核→首次登录流程，增加管理员角色权限、邮箱域名白名单校验、前端体验优化。

## 执行计划

### Step 1：User 模型加 `role` 字段
- `backend/app/models/user.py` — 新增 UserRole 枚举，User 加 role 列
- `backend/alembic/versions/006_user_role.py` — 迁移脚本

### Step 2：后端 `require_admin` 权限校验
- `backend/app/api/admin_users.py` — require_admin 检查 role

### Step 3：`UserResponse` 返回 `status` + `role`
- `backend/app/schemas.py` — UserResponse 加字段
- `backend/app/api/auth.py` — get_me 返回字段

### Step 4：注册 API 加邮箱域名白名单校验
- `backend/app/api/auth.py` — register 校验邮箱后缀

### Step 5：前端 `AuthDialog` 优化
- `src/components/ui/AuthDialog.tsx` — caohua.com 默认、审批状态提示

### Step 6：前端管理后台路由守卫
- `src/app/admin/layout.tsx` — admin 角色检查

### Step 7：前端适配 `status`/`role`
- `src/lib/api/client.ts` — 类型定义
- `src/stores/remoteStore.ts` — store 保存 role

## 状态：执行中

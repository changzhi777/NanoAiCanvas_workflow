# 文生图任务失败诊断测试计划

## 执行时间
2026-05-05 00:12:00

## 问题分析
1. **代码 Bug**：GenerationPanel.tsx 中 `finalPrompt` 在定义前被引用
2. **缺失函数**：suchuang-api.ts 缺少 `getNanoaiResult` 函数

## 已修复
1. ✅ 将 `finalPrompt` 定义移到限制词检查之前（GenerationPanel.tsx:235）
2. ✅ 添加缺失的 `getNanoaiResult` 函数（suchuang-api.ts:92）

## 待验证
1. 构建成功：✓ (vite build)
2. 部署到远程服务器
3. 执行完整测试流程

## 测试流程
1. 前端：使用浏览器 DevTools 跟踪 handleGenerate
2. 后端：`tail -f /tmp/uvicorn.log` 观察请求
3. API：`curl` 测试限制词检查、任务提交、结果轮询

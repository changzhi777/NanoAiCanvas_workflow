# 用户登录优化批次2

## 执行计划

### Step 1: 安全修复 (auth.py)
- logout 销毁 Redis session
- refresh 检查用户状态
- get_current_user 检查 is_active
- 注册密码强度校验

### Step 2: 登录接口返回用户信息
- login 返回 TokenResponse + UserResponse
- 前端 AuthDialog 少一次 auth.me 请求

### Step 3: 头像持久化
- User 模型加 avatar_url 字段 + 迁移
- 新增头像上传 API (POST /auth/avatar)
- UserResponse 返回 avatar_url
- UserSettingsDialog 上传调后端
- LoginButton 从 user 对象读头像

### Step 4: 管理后台按钮 admin 角色可见
### Step 5: ALLOWED_EMAIL_DOMAINS 移到 config

## 状态：执行中

# TVC 模块审查优化与测试

**完成时间**: 2026-05-14 16:46
**状态**: ✅ 已完成

## 任务概述

审查 TVC 模块功能，进行结构化重构，并编写全面测试覆盖。

## 执行内容

### 后端重构
- 拆分 `workflow_tasks.py` (999→210行) → `tvc_engine.py` + `tvc_providers.py` + `tvc_polling.py`
- 积分预扣/退款机制
- asyncio.gather 批量并行
- Redis CAS 乐观锁

### 前端重构
- `useTvcExecution.ts` Hook 抽离
- `tvc-api.ts` API 完善
- `TvcScriptNode.tsx` 纯 UI 化

### 测试覆盖 (57 tests)
- 后端单元: 30 (test_tvc_engine 10 + test_tvc_providers 20)
- 前端单元: 17 (tvc-api 8 + useTvcExecution 9)
- E2E: 10 (tvc-workflow.spec.ts)

全部绿色通过。

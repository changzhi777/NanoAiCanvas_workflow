# TVC 全流程测试与资产保存修复

**完成时间**: 2026-05-14 19:05
**状态**: ✅ 已完成

## 任务概述

实际测试 TVC 模板全工作流，检查资产库是否正确保存生成内容。

## 发现的问题

1. **环境问题**: 远程后端数据库（64.118.135.134）PostgreSQL/Redis 不可达，无法实际执行 AI 生成
2. **代码缺陷**: `StoryboardVideoNode.tsx` 的 `savedToAsset` 字段只有 UI 显示，没有实际资产保存逻辑

## 修复内容

- `src/components/nanoai-workflow/nodes/StoryboardVideoNode.tsx`
  - 导入 `assetsApi` + `AssetCreate` 类型
  - 添加 `useRef` 防重入守卫
  - 视频生成成功后逐个调用 `assetsApi.create` 保存到资产库
  - 保存成功后设置 `savedToAsset: true` 触发 UI 盾牌图标
  - 重新执行时重置 ref

## 资产保存机制梳理

- **图片**: `ImagePreviewNode` 已有完整自动保存（`sourceNodeCompleted` 守卫 + `createImageAssetApi`）
- **视频**: 本次修复后补齐（`enableAssetSave` 开关 + `assetsApi.create` + token 校验）
- **BGM/配音**: 附加在视频资产 meta 中，暂无独立资产保存

## 构建验证

- `vite build` ✅ 通过

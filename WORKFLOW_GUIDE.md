# 📍 NanoAI Workflow 页面位置示意

## 当前页面布局

```
┌─────────────────────────────────────────────────────────┐
│  [无限画布] [NanoAI Workflow]          🌙 🇨🇳 👥 ❓ ❓   │ ← 顶部工具栏
├──────────┬──────────────────────────────────────────────┤
│          │                                               │
│  左侧    │              无限画布区域                      │
│  侧边栏   │         (拖拽节点到此处)                      │
│          │                                               │
│  ┌────┐  │                                               │
│  │节点库│  │   当前状态：                                │
│  └────┘  │   • 0 个节点                                 │
│          │   • 0 条连线                                 │
│  生成节点  │   • 深色主题                                 │
│  ⚡生成   │                                               │
│  📄脚本   │   💡 提示：按 Cmd+T 打开模板对话框            │
│  🎬分镜   │                                               │
│  🎤对白   │                                               │
│  👤角色   │                                               │
│  🏞️场景   │                                               │
│          │                                               │
└──────────┴──────────────────────────────────────────────┘
```

## 🎯 如何打开模板对话框

### 方法 1：快捷键（推荐）
- 按 **`Cmd + T`** (Mac) 或 **`Ctrl + T`** (Windows)
- 直接打开模板选择对话框

### 方法 2：添加按钮
- **当前缺失**：侧边栏没有"添加节点"按钮打开模板
- **临时方案**：使用快捷键 `Cmd+T`

## 📋 模板对话框位置

打开后会看到：

```
┌─────────────────────────────────────────────┐
│           工作流模板                         │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │全部 │ │故事 │ │角色 │ │场景 │ ...      │ ← 分类标签
│  └─────┘ └─────┘ └─────┘ └─────┘          │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │故事板01 │  │角色设计 │  │场景设计 │    │ ← 模板卡片
│  │ ⭐推荐   │  │ 📄4步   │  │ 🏞️3步   │    │
│  │ 9节点   │  │         │  │         │    │
│  └─────────┘  └─────────┘  └─────────┘    │
│                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │快速分镜 │  │完整故事板│ ...          │    │
│  └─────────┘  └─────────┘                │
└─────────────────────────────────────────────┘
```

## 🔍 检查模板数量

在浏览器控制台（F12）运行：

```javascript
// 查看当前模板数量
console.log('模板数量:', window.useNanoaiWorkflowStore?.getState?.()?.templates?.length);

// 查看所有模板
console.log('所有模板:', window.useNanoaiWorkflowStore?.getState?.()?.templates);
```

## 🛠️ 如果模板未显示

1. **刷新页面**：按 `Cmd + R`
2. **清除缓存**：
   ```javascript
   localStorage.clear();
   location.reload();
   ```
3. **检查控制台**：按 F12 查看是否有错误

## 📞 反馈当前状态

请告诉我：
1. ✅ 是否能看到左侧"节点库"侧边栏？
2. ✅ 按 `Cmd+T` 后是否弹出模板对话框？
3. ✅ 模板对话框中显示几个模板？
4. ✅ 控制台是否有错误信息？
